<!DOCTYPE html>
<html>

<head>
    <title></title>
    <style>
        * {margin:0}
        .header-information { display: flex; width:85vw; margin-left: 7.5vw; justify-content: space-between; height: 15vh}
        .murtfeldt-information {color: red, flex: 1}
        .murtfeldt-address { border-bottom: 1px solid black; font-size: smaller; margin-bottom: 4em}
        .logo{ height: 7vh; float: right; margin-bottom: 4em; margin-top: 1.5em; margin-right: 1em}
        footer { display: flex; width:85vw; margin-left: 7.5vw; border-top: 1px solid black; padding-top: 2em; height: 5vh}
        footer div {font-size:smaller; flex-grow: 1}
    </style>
</head>

<body>
    <table>
        <thead>
            <td>
                {% block header %}
                {% endblock header %}
            </td>
        </thead>
        <tbody>
            <td>
                {% block content %}
                {% endblock content %}
            </td>
        </tbody>
        
        <tfoot>
            <td>
                {% block footer %}
                {% endblock footer %}
            </td>
        </tfoot>
    </table>
</body>

</html>